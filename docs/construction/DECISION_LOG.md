# Decision Log

2026-09-07：D001–D020 均来自用户确认。2026-09-10：用户基于成品试用确认 D021–D031。2026-09-11：用户继续确认 D032–D041。2026-09-12：用户确认 D042–D046。以上决定不能由施工 Agent 自行推翻。

| ID | 决定 |
| --- | --- |
| D001 | 新页面叫“探索”，不是“知乎”。 |
| D002 | 第一用户是 Maker / 创客。 |
| D003 | 找灵感和解问题为互补主功能。 |
| D004 | 功能优先级：灵感 > 排障 > OAuth 个性化；OAuth 不在 MVP。 |
| D005 | Demo 尽量展示两条链路，排障为更重要黄金路径。 |
| D006 | 知乎是知识渠道，不是产品主体。 |
| D007 | 真正接入官方 zhihu Skill。 |
| D008 | 不自行重写知乎 API 能力。 |
| D009 | 每个用户配置自己的 Access Secret。 |
| D010 | Explore 只分析和判断，不直接修改硬件工程。 |
| D011 | “交给 Catnip”后先生成执行计划。 |
| D012 | 用户确认后才可修改 / Build / Flash / 验证。 |
| D013 | 排障结合知乎社区经验与全网权威资料交叉验证。 |
| D014 | Catnip 使用自己的本地长期知识库。 |
| D015 | 用户主动收藏，不批量抓取知乎。 |
| D016 | 知识卡记录真实验证状态。 |
| D017 | 历史知识可自动发现，加入分析前由用户决定。 |
| D018 | MVP 不做 OAuth 用户画像。 |
| D019 | MVP 不做 Web 化。 |
| D020 | 复用 Agent、Skill Manager、Runtime MCP、Hardboard。 |
| D021 | 应用每次冷启动必须由用户明确选择现有硬件工程，或在应用解析出的 `hardboard/projects` 下新建工程；不得自动选择列表第一项或沿用旧 Runtime 工程。 |
| D022 | 当前工程是编辑器、监视器/任务证据、任务管理、Explore Context/Handoff 与 Agent 工程执行的单一上下文；项目相关动作不得静默落到其他目录。 |
| D023 | 找灵感与解问题按工程分别保存未完成工作；返回首页、切工作区、reload/restart 不清空，只有用户明确新建/清除才删除；中断请求不得假装仍在运行。 |
| D024 | 选择/切换工程时必须同步切换该工程的 Agent 对话历史、当前会话和上下文；不同工程不可串话，旧全局历史保留为未归属历史且不得自动绑定。 |
| D025 | 编辑器工程树/打开文件与 Build、Flash、Serial 目标及证据过滤必须随当前工程共同切换；运行中或排队任务不得被静默改绑，存在副作用风险时阻止切换或要求明确处理。 |
| D026 | 每个工程的找灵感与解问题必须分别保存多次历史记录，并以受控目录承载已完成、未完成和中断工作；不能只保存一个最新页面状态。 |
| D027 | 工程开发 Agent 对话和持续上下文统一保存到对应工程目录；不同工程必须物理隔离并随工程切换。 |
| D028 | Explore 必须拥有独立 AI 对话记录，并按每条灵感/问题历史保存草稿与对话；不得占用左侧工程开发 Agent 对话。 |
| D029 | 探索四阶段对已到达步骤可随时切换回看；切换不得重新执行搜索、模型或硬件动作。 |
| D030 | 第三阶段由 Explore AI 在工程内分层目录生成计划与交接材料；第四阶段展示工程内材料，用户确认提交后才交给左侧工程 Agent 执行。 |
| D031 | Explore Idea/Source 操作按钮必须尺寸统一、层级清楚；收藏与打开原页面要明显可见。 |
| D032 | 删除既有 Explore/Agent 使用记录、收藏和四个指定 Skill；删除后旧未归属 Agent 历史不得在当前 UI 重新出现。 |
| D033 | 当前公开版本升级为 `v2.0.0`，Build 保持 `7201`。 |
| D034 | 吉祥物与独立软件助手统一为“Neil·Bauman's 学院呱呱”，助手知识库同步更新。 |
| D035 | 探索右侧增加“Neil 的 skill 小站”，固定打开用户指定 Skill Hub，并复用本地 Skill Manager 安装能力；不显示浏览器录制控件。 |
| D036 | 新手引导必须覆盖探索、四阶段门禁、独立历史、Skill 小站和学院呱呱。 |
| D037 | Explore 在生成最终交接材料及确认提交后仍保存所属独立对话和阶段状态，历史可恢复查看。 |
| D038 | Plan/Handoff 材料在执行页可编辑保存并可打开工程目录，但确认提交门禁不变。 |
| D039 | 学院呱呱聊天框顶部提供清晰的作者 GitHub 入口，仅由 Main 白名单在系统浏览器打开固定地址。 |
| D040 | 收藏知识保留受限的来源 Explore 对话快照并提供明确删除；删除收藏与删除 Explore 历史互不级联。 |
| D041 | 找灵感与解问题入口使用同系列学院呱呱场景插画，并保持文字清晰、入口色彩区分和响应式布局。 |
| D042 | 新增第七个可见工作区“模型”，以 CC Switch 类似的信息结构管理供应商、模型档案和用途默认值，但复用现有 Agent/任务系统。 |
| D043 | 工程 Agent 的模型选择按工程和会话持久化，任务创建时冻结非敏感模型快照；禁止静默 fallback。 |
| D044 | 供应商支持按真实协议能力开放；改变 base URL 不等于完成 OpenAI/Anthropic 等协议兼容。 |
| D045 | API Key 与知乎 Access Secret 只由 Main/宿主安全输入和系统凭据存储处理，Renderer 只接收配置状态。 |
| D046 | 知乎连接在成功后仍可维护，替换、验证和退出均调用官方 `zhihu` Skill 已提供的命令。 |

## 工程决定（不改变产品行为）

- E001 / Phase 0：新文档在 `docs/product` 和 `docs/construction` 建立，旧文档原样保留引用。避免把历史接力状态误作本轮事实。
- E002 / Phase 1 计划：标准目录型 Skill 的源文档和部署文档应保持上游原字节；展示元数据的多行 YAML 解析在宿主修复，legacy 兼容仍保留。证据见现场报告；先写失败回归再改宿主，不修改官方协议。
- E003 / Phase 2 候选：调查现有 JSON user-data 后优先选择原子写入的小型 JSON store；在持久化/损坏/重启测试前仍为 A3 假设，不宣称已实现。
- E004 / Phase 3 前置：现有 Agent 跳过权限确认，不能直接接收 Explore 请求；先验证最小受限执行模式和结构化输出能力，复用队列而不造通用协议。门禁未通过不开放交接。
